// const buttons = document.querySelectorAll("button");

// buttons.forEach(button => {
//     button.addEventListener("click", function() {
//         alert("button bagus!");
//     });
// });

const btnEl = document.querySelectorAll("button#btn-el");

btnEl.forEach(btnEl => {
    btnEl.addEventListener("click", function () {
        alert("Button Bagus!");
    });
});